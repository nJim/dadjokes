# import * from './generated/prisma.graphql'

type Mutation {
  createJoke(content: String!): Joke!
  updateJoke(id: ID!, content: String): Joke!
  deleteJoke(id: ID!): Joke
}

type Query {
  jokes: [Joke]!
  joke(where: JokeWhereUniqueInput!): Joke
}
