import React from 'react';
import {DeleteButton} from '.';

export default {
  title: 'Atoms|Buttons',
};

export const deleteButton = () => (
  <DeleteButton id="delete">
    Delete
  </DeleteButton>
);
