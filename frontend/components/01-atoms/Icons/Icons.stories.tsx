import React from 'react';
import {LikeIcon, DislikeIcon, SettingsIcon, ShareIcon} from '.';

export default {
  title: 'Atoms|Icons',
};

export const likeIcon = () => (
  <LikeIcon />
);

export const dislikeIcon = () => (
  <DislikeIcon />
);

export const settingsIcon = () => (
  <SettingsIcon />
);

export const shareIcon = () => (
  <ShareIcon />
);

